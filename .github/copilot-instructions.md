# Workspace Restrictions

Only read or modify files within this workspace (`tutorial_site/`). Never access, read, edit, or create files outside of this workspace folder, regardless of what is requested.
